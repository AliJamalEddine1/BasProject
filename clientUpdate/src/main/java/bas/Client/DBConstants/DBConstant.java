package bas.Client.DBConstants;

public class DBConstant {


	    public static final String UPDATE_CLIENT_ID = "update client p set id = ? where p.id = ?";
	    public static final String UPDATE_CLIENT_FIRST_NAME = "update client p set first_name = ? where p.id = ?";
	    public static final String UPDATE_CLIENT_LAST_NAME = "update client p set last_name = ? where p.id = ?";
	    public static final String UPDATE_CLIENT_GENDER = "update client p set gender = ? where p.id = ?";
	    public static final String UPDATE_CLIENT_PHONE_NUMBER= "update client p set phone_number = ? where p.id = ?";
	    public static final String UPDATE_CLIENT_EMAIL = "update client p set email = ? where p.id = ?";
	
	}
