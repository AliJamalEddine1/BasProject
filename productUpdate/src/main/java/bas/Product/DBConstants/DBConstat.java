package bas.Product.DBConstants;

public class DBConstat {


    public static final String UPDATE_PRODUCT_ID = "update product p set id = ? where p.id = ?";
    public static final String UPDATE_PRODUCT_CLIENTID = "update product p set clientid = ? where p.id = ?";
    public static final String UPDATE_PRODUCT_TYPE = "update product p set type = ? where p.id = ?";
    public static final String UPDATE_PRODUCT_QUANTITY = "update product p set qty = ? where p.id = ?";
    public static final String UPDATE_PRODUCT_PRICE = "update product p set price = ? where p.id = ?";
    public static final String UPDATE_PRODUCT_LOCATION= "update product p set location = ? where p.id = ?";
    public static final String UPDATE_PRODUCT_IMAGE = "update product p set image = ? where p.id = ?";
    public static final String UPDATE_PRODUCT_DESCRIPTION = "update product p set description = ? where p.id = ?";
    public static final String UPDATE_PRODUCT_YEAR = "update product p set yearOf = ? where p.id = ?"; 
}

